name: Windows CI

on:
  push:
    branches: [v1.0.0]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: build-matrix
        run: |
          python tools/build_matrix/build_matrix.py
