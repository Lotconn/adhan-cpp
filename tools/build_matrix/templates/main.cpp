// This program intentionally does nothing except link against the
// installed package. Successfully compiling and linking is the whole
// point of this check, so it stays free of any project-specific headers.
int main() {
  return 0;
}